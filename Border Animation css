<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Border animation</title>
    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        .box{
            transition: 1s;
            width: 400px;
            height: 100px;
            font-size: 30px;
            font-weight: 900;
            position: absolute;
            top: 40%;
            left: 40%;
            background-color: black;
            color: white;
           justify-content: center;
           align-items: center;
            display: flex;
           overflow: hidden;
        }
       body{
        background-color: rgb(0, 0, 0);
       }
       .box::before{
        content: '';
        position: absolute;
       }
       .box span:nth-child(1){
        width: 100%;
        height: 5px;
        background: linear-gradient(to right ,white,blue);
        position: absolute;
        top:0;
        left: 0;
        animation: first 2s linear infinite;
       }
       @keyframes first {
        0%{
            transform: translateX(-100%);
        }
        100%{
            transform: translateX(100%);
        }
       }
       .box span:nth-child(2){
        height: 100%;
        width: 5px;
        background: linear-gradient(to bottom ,white,blue);
        position: absolute;
        right: 0;
        animation: second 1.5s linear infinite 1s;
       }
       @keyframes second {
        0%{
            transform: translateY(-100%);
        }
        100%{
            transform: translateY(100%);
        }
       }     
       .box span:nth-child(3){
        width: 100%;
        height: 5px;
        background: linear-gradient(to left ,white,blue);
        position: absolute;
        bottom: 0;
        animation: third 2s linear infinite;
       }
       @keyframes third {
        0%{
            transform: translateX(100%);
        }
        100%{
            transform: translateX(-100%);
        }
       }     
       .box span:nth-child(4){
        height: 100%;
        width: 5px;
        background: linear-gradient(to top ,white,blue);
        position: absolute;
        left: 0;
        animation: forth 1.5s linear infinite 1s;
       }
       @keyframes forth {
        0%{
            transform: translatey(100%);
        }
        100%{
            transform: translatey(-100%);
        }
       }
       .box:hover{
        background-color: blue;
        cursor: pointer;
        box-shadow: 8px 8px 8px rgb(255, 1, 1),
                   -8px -8px 8px rgb(255, 0, 0),
                   -8px 8px 8px rgb(255, 0, 0),
                   8px -8px 8px rgb(255, 0, 0);
         color: chartreuse;          
       }
       .box:hover span{
        background: linear-gradient(blue,blue);
       }
      
    </style>
</head>
<body>
    <div class="container">
        <div class="box">
            <span></span>
            <span></span>
            <span></span>
            <span></span>
            THE RS PLATEFORM
        </div>
    </div>
</body>
</html>
